# ============================================================
# Environment Variables — Copy to .env and fill in your values
# ============================================================
# cp .env.example .env
# NEVER commit .env to git (it's in .gitignore)

# --- Logging ---
# DEBUG, INFO (default), WARNING, ERROR
LOG_LEVEL=INFO

# --- Cover Letter Generation (required) ---
# Free: https://console.groq.com/keys → Create API Key
GROQ_API_KEY=
GROQ_LLM_MODEL=llama-3.3-70b-versatile

# --- Job Search APIs (at least one recommended) ---
# SerpAPI (Google Jobs): https://serpapi.com → 100 free searches/month
SERPAPI_KEY=
# JSearch (RapidAPI): https://rapidapi.com/letscrape-6bRDu3Sgupt/api/jsearch
JSEARCH_API_KEY=
# Adzuna: https://developer.adzuna.com/ → 250 free requests/day
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
# LinkedIn Jobs (RapidAPI): https://rapidapi.com/jaypat87/api/linkedin-jobs-search
RAPIDAPI_KEY=
# Remotive: free, no key needed — auto-enabled when "Remote" is in your locations

# --- Browser Automation Credentials (optional) ---
# Used by the agent to auto-apply on job sites
# LinkedIn Easy Apply
LINKEDIN_EMAIL=
LINKEDIN_PASSWORD=
# Naukri Apply
NAUKRI_EMAIL=
NAUKRI_PASSWORD=
# Generic career sites (Workday, Greenhouse, Lever, etc.)
APPLY_EMAIL=
APPLY_PASSWORD=

# --- Email Report (optional) ---
# Gmail: use an App Password → https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=
TO_EMAIL=

# --- Schedule ---
# Hour (IST) for daily cron run (default: 11 AM)
DAILY_RUN_HOUR_IST=11

# --- Credential Encryption ---
# Set this to auto-decrypt .env.enc without prompting (for cron jobs)
# MASTER_PASSWORD=
